# Copy this file to .env and fill in your backend API URL.
# Replace the IP address below with your machine's local network IP
# so that Expo Go on your phone can reach the backend.
#
# Find your local IP with: ipconfig (Windows) or ifconfig / ip a (macOS/Linux)

EXPO_PUBLIC_API_URL=http://192.168.1.100:8000/api
